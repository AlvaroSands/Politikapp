# Copia este archivo a .env y rellena los valores.
# El archivo .env real nunca se sube a GitHub (está en .gitignore).
# En producción, configura estas variables en el panel de Railway.

# NewsAPI (opcional — para feeds adicionales en el futuro)
# NEWS_API_KEY=tu_clave_aqui
